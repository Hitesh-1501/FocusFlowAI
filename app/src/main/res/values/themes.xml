<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FocusFlowAI" parent="Theme.Material3.Light.NoActionBar">

        <item name="colorPrimary">@color/primaryBlue</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/primaryBlue</item>
        <item name="android:fontFamily">@font/poppins</item>

    </style>

    <style name="Theme.FocusFlowAI" parent="Base.Theme.FocusFlowAI" />

    <style name="CircularProgress">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/circular_progress</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:minWidth">120dp</item>
        <item name="android:minHeight">120dp</item>
        <item name="android:maxWidth">120dp</item>
        <item name="android:maxHeight">120dp</item>
    </style>

</resources>